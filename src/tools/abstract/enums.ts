// eslint-disable-next-line import/prefer-default-export
export enum EModules {
  Logs = 'logs',
  Stats = 'stats',
  Users = 'users',
  Party = 'party',
  Profiles = 'profiles',
  Inventory = 'inventory',
  BugReport = 'bugReport',
  LoginAttempt = 'loginAttempt',
}
